import { PageHero } from "@/components/layout/page-hero";

export function DashboardHero() {
  return (
    <PageHero
      title="Friend boards"
      description="Side-by-side RetroAchievements high scores for your tracked games. Rank is among friends only."
    />
  );
}
